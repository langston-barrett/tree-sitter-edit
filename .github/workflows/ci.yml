name: ci
on:
  push:
    branches:
    - main
  pull_request:

permissions:
  contents: read

jobs:
  ci:
    uses: langston-barrett/gha/.github/workflows/rust-ci.yml@fc3619dd7d8730f88eac66bbf15f1257de683acc
    with:
      bench: false
      os: ubuntu-latest