name: ci
on:
  push:
    branches:
    - main
  pull_request:

permissions:
  contents: read

jobs:
  ci:
    uses: langston-barrett/gha/.github/workflows/rust-ci.yml@585d8ef2d04787e911c2ff3bfbda30e5b3d620f3
    with:
      bench: false
      os: ubuntu-latest