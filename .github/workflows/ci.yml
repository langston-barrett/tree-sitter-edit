name: ci
on:
  push:
    branches:
    - main
  pull_request:

permissions:
  contents: read

jobs:
  ci:
    uses: langston-barrett/gha/.github/workflows/rust-ci.yml@aff5ad2d15466eedbc666f71fb641c1ff1b4b4b5
    with:
      bench: false
      os: ubuntu-latest